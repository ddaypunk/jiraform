import csv, re, sys
# csv used to read in test case lines from csv file
# re used to detect numbers within the lines

# take note of the filename
test_cases = str(sys.argv[1])

#open the csv and read from it
# https://docs.python.org/2/library/csv.html
with open(test_cases, )